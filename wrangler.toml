name = "devbyshima"
main = "src/worker.ts"
compatibility_date = "2024-09-23"

# Served from the free workers.dev subdomain by default. After your first
# `pnpm deploy`, wrangler prints the real URL — it includes your account
# subdomain, e.g. https://devbyshima.<your-account>.workers.dev
#
# To use a custom domain later, add (and make sure the zone is on Cloudflare):
# routes = [{ pattern = "readme.byshima.dev", custom_domain = true }]
